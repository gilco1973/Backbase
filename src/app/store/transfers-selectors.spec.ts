import { TransfersSelectors } from './transfers-selectors';

describe('TransfersSelectors', () => {
  it('should create an instance', () => {
    expect(new TransfersSelectors()).toBeTruthy();
  });
});
